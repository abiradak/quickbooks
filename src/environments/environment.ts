// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  // apiUrl: 'https://www.demo.com/api/',
  // orgId: '2da5e7ac-f5de-4c07-814f-aa3398d5ffa8',
  // BASE_API_ENDPOINT: 'http://api.greentowncanada.local:5336',
  // AUTH_URL: 'http://auth.greentowncanada.local:5433'

  // apiUrl: 'https://www.demo.com/api/',
  orgId: '9453b7b4-87dc-4f9f-88a5-e1f93530c12c',
  BASE_API_ENDPOINT: 'https://portal.vnnergy.com',
  AUTH_URL: 'https://auth.vnnergy.com',
  approvalRequired: true,
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
